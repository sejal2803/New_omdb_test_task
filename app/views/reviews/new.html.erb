<h1>Add Review</h1>

<%= form_with(model: @review, url: { action: 'create' }, local: true, html: { class: "form" }) do |form| %>
  <div class="form-group mb-3">
    <%= form.text_field :body, class: "form-control" %>
  </div>
  <%= form.submit "Submit", class: "btn btn-primary" %>
<% end %>
